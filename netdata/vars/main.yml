---
# Netdata version to be installed
_netdata_version: 1.6.0

# Netdata URL download from
_netdata_binary_name: "netdata-{{ _netdata_version }}-binary.tar.bz2"

# Netdata destination directory unpack to
_netdata_dir: "/opt/netdata-{{ _netdata_version }}"
